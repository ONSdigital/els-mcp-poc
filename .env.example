# Base URL of the ELS API, no trailing slash, including the /api/v1 path prefix.
#
# Currently a Vercel branch preview of the (still-unmerged) api-improvements branch of
# ONSdigital/explore-local-statistics-app — see CLAUDE.md. Swap this for the production ELS API
# base URL once that branch merges; this must stay a config change, not a code edit.
ELS_API_BASE_URL=https://els-app-mcp.vercel.app/api/v1
